apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "marketdata.fullname" . }}-config
  labels:
    {{- include "marketdata.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"