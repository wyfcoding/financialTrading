apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "monitoringanalytics.fullname" . }}-config
  labels:
    {{- include "monitoringanalytics.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"