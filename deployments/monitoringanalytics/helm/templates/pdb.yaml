apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: { include "monitoringanalytics.fullname" . }
  labels:
    {- include "monitoringanalytics.labels" . | nindent 4 }
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {- include "monitoringanalytics.selectorLabels" . | nindent 6 }
