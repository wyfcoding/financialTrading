apiVersion: v1
kind: Service
metadata:
  name: {{ include "primebrokerage.fullname" . }}
  labels:
    {{- include "primebrokerage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "primebrokerage.selectorLabels" . | nindent 4 }}
