apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "marketsimulation.fullname" . }}-config
  labels:
    {{- include "marketsimulation.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"