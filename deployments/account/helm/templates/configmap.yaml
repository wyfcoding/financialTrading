apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "account.fullname" . }}-config
  labels:
    {{- include "account.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"