apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "marketmaking.fullname" . }}-config
  labels:
    {{- include "marketmaking.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"