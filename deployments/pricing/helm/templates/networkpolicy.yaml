apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: { include "pricing.fullname" . }
  labels:
    {- include "pricing.labels" . | nindent 4 }
spec:
  podSelector:
    matchLabels:
      {- include "pricing.selectorLabels" . | nindent 6 }
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector: {}
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
