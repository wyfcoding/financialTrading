apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "risk.fullname" . }}-config
  labels:
    {{- include "risk.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"