apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "quant.fullname" . }}-config
  labels:
    {{- include "quant.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"