apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "position.fullname" . }}-config
  labels:
    {{- include "position.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"