apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config
  labels:
    app: {{ .Chart.Name }}
data:
  config.toml: |
    [server]
    grpc_port = "{{ .Values.config.server.grpc_port }}"

    [log]
    level = "{{ .Values.config.log.level }}"
    format = "{{ .Values.config.log.format }}"

    [kafka]
    brokers = {{ .Values.config.kafka.brokers | toJson }}
    topic_trade = "{{ .Values.config.kafka.topic_trade }}"
    topic_order = "{{ .Values.config.kafka.topic_order }}"