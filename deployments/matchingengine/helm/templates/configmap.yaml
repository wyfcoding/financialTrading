apiVersion: v1
kind: ConfigMap
metadata:
  name: { include "matchingengine.fullname" . }-config
  labels:
    {- include "matchingengine.labels" . | nindent 4 }
data:
  # Placeholder for config.toml content or other config
  config.toml: |
    [server]
    name = "{ .Values.image.repository }"
    environment = "prod"
