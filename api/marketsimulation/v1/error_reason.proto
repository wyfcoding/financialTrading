syntax = "proto3";

package marketsimulation.v1;

import "google/protobuf/descriptor.proto";

option go_package = "github.com/wyfcoding/financialtrading/go-api/marketsimulation/v1;v1";

enum ErrorReason {
  // Common errors
  GE_UNSPECIFIED = 0;
  GE_UNKNOWN_ERROR = 1;
  GE_INVALID_ARGUMENT = 2;
  GE_RECORD_NOT_FOUND = 3;

  // Domain specific errors
  SIMULATION_ALREADY_RUNNING = 101;
  SIMULATION_NOT_FOUND = 102;
  SIMULATION_CONFIG_INVALID = 103;
}
